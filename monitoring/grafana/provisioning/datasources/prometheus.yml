# Datasource провижнится из репо: uid фиксированный, на него ссылаются
# дашборды в monitoring/grafana/dashboards/.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
