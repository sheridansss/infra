# Автоопределение текстовых файлов, переводы строк нормализуются в LF в репозитории
* text=auto

# bash-скрипты обязаны быть с LF даже в рабочей копии на Windows,
# иначе bash падает на '\r'
*.sh text eol=lf
